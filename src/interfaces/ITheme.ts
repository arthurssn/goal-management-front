export interface Theme {
  name: "defaultTheme";
  colors: {
    red: string;
    white: string;

    darkTextColor: string;
    lightTextColor: string;
  };

  alertsColor: {
    warning: string;
  };
}
